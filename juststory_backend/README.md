# JustStory Backend

Backend приложение для игры-сторителлера на базе NestJS с интеграцией GigaChat AI для генерации интерактивных историй.

## 📋 Описание

JustStory - это backend API для интерактивной игры-сторителлера, где пользователи могут создавать и развивать истории с помощью искусственного интеллекта. Приложение использует GigaChat для генерации сюжетов, вариантов действий и продолжений историй.

## 🚀 Технологии

- **NestJS** - прогрессивный Node.js фреймворк
- **Prisma** - ORM для работы с базой данных
- **SQLite** - база данных
- **Redis** - кеширование данных
- **GigaChat** - AI для генерации контента
- **JWT** - аутентификация пользователей
- **Argon2** - хеширование паролей
- **Swagger** - API документация

## 📦 Установка

### Требования

- Node.js (версия 14 или выше)
- npm или yarn
- Docker (для Redis)

### Шаги установки

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd juststory_backend
```

2. Установите зависимости:
```bash
npm install
```

3. Настройте переменные окружения:
Создайте файл `.env` в корне проекта:
```env
AI_TOKEN=your_gigachat_token
JWT_SECRET=your_jwt_secret_key
DATABASE_URL="file:./dev.db"
```

4. Инициализируйте базу данных:
```bash
npx prisma generate
npx prisma migrate dev
```

5. Запустите Redis:
```bash
docker run --name redis -p 6379:6379 -d redis
```

## 🏃 Запуск приложения

### Режим разработки
```bash
npm run start:dev
```

### Режим продакшн
```bash
npm run build
npm run start:prod
```

Приложение будет доступно по адресу: `http://localhost:4202`

## 📚 API Документация

После запуска приложения, Swagger UI доступен по адресу:
```
http://localhost:4202/swagger
```

## 🔧 Основные модули

### AI Module (`/api/ai`)
- `POST /api/ai/send-message-start` - Генерация начальной истории
- `POST /api/ai/send-message` - Продолжение истории на основе выбора пользователя
- `POST /api/ai/get-actions` - Получение вариантов действий для истории

**Особенности:**
- Автоматические повторные попытки при сетевых ошибках
- Обработка ошибок 429 (Too Many Requests) с экспоненциальной задержкой
- Retry логика с до 3 попыток

### User Module (`/api/user`)
- Регистрация и аутентификация пользователей
- JWT токены для авторизации
- Управление профилями пользователей
- Кеширование данных пользователей в Redis

### Subscription Module (`/api/subscription`)
- Управление подписками
- Покупка подписок
- Проверка статуса подписки
- Автоматическая проверка истечения подписок

## 🗄️ База данных

Проект использует Prisma ORM с SQLite. Схема базы данных:

### User
- `id` - Уникальный идентификатор
- `name` - Имя пользователя (уникальное)
- `password` - Хешированный пароль
- `token` - JWT токен
- `role` - Роль пользователя (User/Admin)
- `subscription` - Статус подписки
- `subBuyTime` - Дата покупки подписки
- `subEndTime` - Дата окончания подписки

### Subscription
- `id` - Уникальный идентификатор
- `name` - Название подписки
- `price` - Цена
- `daysPeriod` - Период действия в днях
- `description` - Описание подписки

### Работа с базой данных

Просмотр данных через Prisma Studio:
```bash
npx prisma studio
```

Создание новой миграции:
```bash
npx prisma migrate dev --name migration_name
```

## 🔐 Аутентификация

Приложение использует JWT токены для аутентификации. Токены действительны в течение 1 часа.

### Дефолтные администраторы

При первом запуске создаются два администратора:
- **adminlev** / **qwerty**
- **adminzhenya** / **qwerty2** (с активной подпиской)

## ⚙️ Конфигурация

### Переменные окружения

| Переменная | Описание | Обязательная |
|------------|----------|--------------|
| `AI_TOKEN` | Токен для GigaChat API | Да |
| `JWT_SECRET` | Секретный ключ для JWT | Да |
| `DATABASE_URL` | URL базы данных | Да |

### Retry логика для AI запросов

- Максимум попыток: 3
- Начальная задержка: 1 секунда
- Максимальная задержка: 10 секунд (20 секунд для 429 ошибок)
- Экспоненциальная задержка между попытками

## 📁 Структура проекта

```
src/
├── ai/                    # AI модуль (GigaChat интеграция)
│   ├── ai.controller.ts
│   ├── ai.service.ts
│   └── ai.module.ts
├── user/                  # Модуль пользователей
│   ├── user.controller.ts
│   ├── user.service.ts
│   └── user.module.ts
├── subscription/          # Модуль подписок
│   ├── subscription.controller.ts
│   ├── subscription.service.ts
│   └── subscription.module.ts
├── subscription-checker/  # Проверка подписок
├── prisma/               # Prisma конфигурация
│   ├── schema.prisma
│   └── prisma.service.ts
├── redis/                # Redis модуль
├── app.module.ts         # Главный модуль
└── main.ts              # Точка входа
```

## 🐳 Docker

### Redis
```bash
docker run --name redis -p 6379:6379 -d redis
```

### Grafana (опционально)
```bash
docker run -d -p 3030:3030 \
  --name=grafana \
  -e "GF_SECURITY_ADMIN_PASSWORD=admin" \
  -v grafana-storage:/var/lib/grafana \
  grafana/grafana:latest
```

## 🛠️ Скрипты

- `npm run build` - Сборка проекта
- `npm run start` - Запуск в production режиме
- `npm run start:dev` - Запуск в режиме разработки с hot-reload
- `npm run start:debug` - Запуск в режиме отладки
- `npm run lint` - Проверка кода линтером
- `npm run format` - Форматирование кода

## 🐛 Обработка ошибок

Приложение включает продвинутую обработку ошибок:

- **Сетевые ошибки**: Автоматические повторные попытки при таймаутах и разрывах соединения
- **429 Too Many Requests**: Увеличенные задержки с учетом заголовка `retry-after`
- **Другие ошибки**: Понятные сообщения об ошибках для клиента

## 📝 Лицензия

UNLICENSED

## 👤 Автор

CheateRYT - https://github.com/CheateRYT

## 🤝 Вклад

Вклад в проект приветствуется! Пожалуйста, создавайте issues и pull requests.
